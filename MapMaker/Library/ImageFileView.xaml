<UserControl x:Class="MapMaker.Library.ImageFileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MapMaker.Library"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance local:ImageFile}">
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" MouseDown="UIElement_OnMouseDown">
        <Image Width="70" Height="70" Stretch="Uniform" Source="{Binding Path}"/>
        <StackPanel>
            <TextBlock FontWeight="Bold" Text="{Binding ShortName}"/>
            <StackPanel Orientation="Horizontal">
                <TextBlock>
                <Run Text="Units: " />
                <Run Text="{Binding PixelWidth}" />
                <Run Text="x" />
                <Run Text="{Binding PixelHeight}" />
            </TextBlock>
            <TextBlock>
                <Run Text="Pixels: " />
                <Run Text="{Binding PixelWidth}" />
                <Run Text="x" />
                <Run Text="{Binding PixelHeight}" />
            </TextBlock>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock>
                    <Run Text="File Type: " />
                    <Run Text="{Binding FileExtension}" />
                </TextBlock>
                <TextBlock>
                    <Run Text="File Size: " />
                    <Run Text="{Binding FileSize}" />
                </TextBlock>
            </StackPanel>
        </StackPanel>
        
    </StackPanel>
</UserControl>
