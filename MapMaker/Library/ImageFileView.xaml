<UserControl x:Class="MapMaker.Library.ImageFileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MapMaker.Library"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance local:ImageFile}">
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" MouseDown="OnMouseDown">
        <Image Width="70" Height="70" Stretch="Uniform" Source="{Binding Path}"/>
        <StackPanel>
            <TextBlock FontWeight="Bold" Text="{Binding ShortName}"/>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Units: " />
                <TextBlock Text="{Binding PixelWidth, Converter={StaticResource GridConverter}}" />
                <TextBlock Text="x" />
                <TextBlock Text="{Binding PixelHeight, Converter={StaticResource GridConverter}}" />
                <TextBlock Text="  Pixels: " />
                <TextBlock Text="{Binding PixelWidth}" />
                <TextBlock Text="x" />
                <TextBlock Text="{Binding PixelHeight}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="File Type: " />
                <TextBlock Text="{Binding FileExtension}" />
                <TextBlock Text="  File Size: " />
                <TextBlock Text="{Binding FileSize}" />
            </StackPanel>
        </StackPanel>
        
    </StackPanel>
</UserControl>
